# 桌面应用启动优化 - 最终方案

## 目标
启动时间 ≤ 5s。

## 优化前实测
- dsh 后端启动：热启动 12–14s，冷启动 50–94s（Windows Defender 扫描 ~34k 个 vendor 文件）
- 到窗口可用总耗时：热启动 ~15s，冷启动数分钟

## 最终方案（按影响排序）

### 1. Windows Defender 排除（冷启动 50–94s → 热启动 ≈ 5s）
`scripts/add-defender-exclusions.ps1` 通过 Add-MpPreference 为 vendor 目录、
`~/.dsh`、安装目录添加排除项。

### 2. 后台服务常驻复用（再次打开 ≈ 2.2s）
dsh 后端以 detached 方式启动（日志写文件、unref），应用退出后继续存活。
状态记录在 `%APPDATA%\deepseek-harness-desktop\server.json`（url + pid）。
启动时按顺序：
1. 若已记录的后端存活且 HTTP 可响应 → 直接复用（约 0.1s）；
2. 否则若有"后端启动中"标记 → 最多等 25s 复用预热中的后端（见第 3 点）；
3. 否则全新启动后端。

托盘菜单提供两种退出：退出（保持后台服务）、停止后台服务并退出
（taskkill 强杀后端，含复用场景）。

### 3. 开机自启后端预热（重启后第一次打开 ≈ 2.2s）
新增 `--backend-only` 静默模式：Windows 登录时无窗口启动，拉起后端、
写入 server.json 后立即退出（约 5s，全程不可见）。通过
app.setLoginItemSettings 注册到 HKCU Run，默认开启，可在托盘菜单
"开机自启后台服务"一键关闭。预热实例不抢占单实例锁——预热期间用户
打开软件不会被吞掉，界面进程会等待预热完成并复用；onServerReady 中的
竞态处理保证任何时刻只有一个后端存活。

## 实测数据（开发模式、热缓存、已加排除）
| 场景 | 到窗口可用 |
| --- | --- |
| 全新启动（无后端） | ~7.4s |
| 登录预热（--backend-only，后台不可见） | ~5.2s 完成 |
| 复用常驻后端 | **~2.2s** ✓ |
| 预热期间打开（最坏竞态） | ~9.5s |

## 权衡
后端常驻内存约 150–250MB，托盘菜单可关闭。
